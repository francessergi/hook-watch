services:
  postgres:
    image: postgres:16-alpine
    container_name: capn-hook-postgres
    environment:
      POSTGRES_DB: capn-hook
      POSTGRES_USER: capn-hook
      POSTGRES_PASSWORD: capn-hook
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U capn-hook -d capn-hook"]
      interval: 5s
      timeout: 5s
      retries: 10

  rabbitmq:
    image: rabbitmq:3-management
    container_name: capn-hook-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: ./api
    container_name: capn-hook-api
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    restart: unless-stopped
    env_file:
      - ./api/.env
    environment:
      DATABASE_URL: postgresql://capn-hook:capn-hook@postgres:5432/capn-hook?serverVersion=16&charset=utf8
      MESSENGER_TRANSPORT_DSN: amqp://guest:guest@rabbitmq:5672/%2f
      DEFAULT_URI: http://localhost:8000
      APP_ENV: dev
      APP_SECRET: dev-secret-change-me
    ports:
      - "8000:8000"
    command: php -S 0.0.0.0:8000 -t public

  worker:
    build:
      context: ./api
    container_name: capn-hook-worker
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    restart: unless-stopped
    env_file:
      - ./api/.env
    environment:
      DATABASE_URL: postgresql://capn-hook:capn-hook@postgres:5432/capn-hook?serverVersion=16&charset=utf8
      MESSENGER_TRANSPORT_DSN: amqp://guest:guest@rabbitmq:5672/%2f
      APP_ENV: dev
      APP_SECRET: dev-secret-change-me
    command: php bin/console messenger:consume async -vv

volumes:
  postgres_data:
